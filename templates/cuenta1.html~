
<div class="column">
		<div class="card" id="NotiListcuentas">
				<header class="">
						<div class="hero is-link">Cuentas Pendientes</div>
				</header>
				{% for cuenta in listacuenta %}
				{% if cuenta[5] == 0 %}
				<div class="box card_boom contraste mt-1 mb-1" id="{{cuenta[2]}}">{{cuenta|safe}}</div>
				{% endif%}
				{% endfor %}
		</div>
</div>
